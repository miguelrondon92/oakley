# Git hooks for Oakley (optional, user-enabled)
#
# Agents do NOT run this for you. Enable locally once:
#   git config core.hooksPath .githooks
#
# pre-commit — blocks .env, keys, and live API key strings from commits
